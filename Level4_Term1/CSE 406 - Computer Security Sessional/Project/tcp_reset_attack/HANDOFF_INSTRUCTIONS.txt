TCP RESET ATTACK - AI AGENT HANDOFF INSTRUCTIONS
=================================================
BUET CSE406 Computer Security Sessional Project
Date: July 28, 2025

CURRENT STATUS:
- Project has been EXTENSIVELY DEVELOPED but may need refinements
- Core attack logic implemented but may require debugging/optimization
- Windows environment has known compatibility issues with Scapy
- Need to migrate to WSL/Linux and potentially fix remaining issues
- Code may need additions, modifications, or complete rewrites during execution

BACKGROUND CONTEXT:
===================
This is a complete TCP Reset Attack implementation for educational purposes.
The attack uses ARP poisoning + TCP RST packet injection to disrupt YouTube traffic.

PROJECT STRUCTURE:
- src/tcp_reset_attack.py (main attack controller)
- src/arp_poisoner.py (ARP poisoning module)  
- src/packet_monitor.py (traffic monitoring + RST injection)
- src/network_utils.py (network utilities - FIXED for Windows)
- src/config_manager.py (configuration management)
- config/attack_config.ini (default settings)
- preflight_check.py (environment validation)
- requirements.txt (Python dependencies)

CURRENT NETWORK SETUP:
=====================
- Laptop: Windows machine with hotspot enabled
- Laptop Hotspot IP: 192.168.137.1
- Phone connected to hotspot: 192.168.137.44 (MAC: fa-3b-33-36-3b-9a)
- Attack Goal: Disrupt YouTube on phone using laptop as attacker

ISSUES ENCOUNTERED:
==================
1. Windows MSYS2 + Scapy has interface compatibility problems
2. Windows GUID interface names not recognized by Scapy
3. ARP poisoning fails with "Interface not found" errors
4. Potential bugs or incomplete implementations in the codebase
5. Attack logic may need refinement or complete rewrite
6. Packet injection timing and sequence calculations may need fixes
7. Error handling and edge cases may need improvement
8. Need to switch to WSL/Linux for proper execution AND debugging

WORKING SOLUTION:
================
Switch to WSL (Windows Subsystem for Linux) where:
- Interface names are Linux-style (eth0, wlan0, etc.)
- Scapy works natively with raw sockets
- ARP poisoning functions as designed
- sudo provides necessary privileges

WSL EXECUTION & DEBUGGING STEPS:
================================
1. Open WSL terminal
2. Navigate to project:
   cd "/mnt/e/BUET Academics/Level 4_Term 1/Lab Cources/CSE 406 - Computer Security Sessional/Project/tcp_reset_attack"

3. Install dependencies:
   sudo apt update
   sudo apt install python3-pip
   pip3 install scapy netifaces colorama

4. FIRST: Analyze the codebase for potential issues:
   - Review all Python files for bugs, incomplete logic, or errors
   - Check packet crafting, sequence number calculations
   - Validate threading implementation and synchronization
   - Examine error handling and edge cases
   - Test individual components before full attack

5. Run preflight check and analyze results:
   sudo python3 preflight_check.py

6. FIX any issues found in step 4 and 5:
   - Modify existing code as needed
   - Add missing functionality
   - Rewrite components if necessary
   - Implement proper error handling

7. Test attack execution:
   sudo python3 src/tcp_reset_attack.py --target "192.168.137.44" --gateway "192.168.137.1" --duration 3

8. If issues arise, DEBUG and ITERATE:
   - Analyze error messages and logs
   - Add debugging output to identify problems
   - Modify packet crafting or timing logic
   - Test with different parameters or approaches

EXPECTED BEHAVIOR (AFTER FIXES):
================================
- ARP poisoning should start successfully (no interface errors)
- Should see "Poisoning packets sent: X" messages
- TCP packet monitoring should capture real YouTube traffic
- RST packets should be crafted with correct sequence numbers
- YouTube on phone (192.168.137.44) should stop working
- Attack auto-terminates after 3 minutes
- Final statistics should show real packet counts (not zeros)

NOTE: If any of these don't work, the code needs debugging/fixing!

TECHNICAL FIXES ALREADY IMPLEMENTED:
====================================
- Enhanced get_mac_address() with 3-tier Windows fallback
- Added get_local_mac_address() for Windows compatibility  
- Improved ARP poisoning with multiple discovery methods
- Cross-platform network utilities
- Basic error handling and logging

POTENTIAL AREAS NEEDING ATTENTION:
=================================
- TCP sequence number calculation accuracy
- RST packet timing and injection logic
- Thread synchronization between ARP poisoning and packet monitoring
- YouTube traffic detection patterns (might need updating)
- Packet filtering and target identification
- Error recovery and graceful failure handling
- Network interface detection in Linux environment
- Raw socket permissions and privilege handling

VERIFICATION CRITERIA:
=====================
Success indicators:
✅ No "Interface not found" errors
✅ ARP poisoning packets being sent
✅ YouTube disruption on target phone
✅ Non-zero statistics in final output
✅ Clean termination after duration

PROJECT COMPLETION STATUS:
==========================
- Code Foundation: 80% Complete ⚠️
- Windows Compatibility: Addressed but untested ⚠️
- Core Attack Logic: Implemented but may need refinement ⚠️
- Linux/WSL Execution: Pending testing and fixes 🔄
- Educational Objectives: Ready to achieve after debugging ✅

REALISTIC EXPECTATIONS:
======================
- The attack framework is built but likely needs debugging
- Execution in WSL may reveal issues that need fixing
- Code modifications, additions, or rewrites may be necessary
- Success requires iterative testing and improvement
- Multiple debugging sessions may be needed

NEXT ACTIONS FOR AI AGENT:
==========================
1. Help user set up WSL environment
2. Guide through dependency installation
3. ANALYZE the codebase thoroughly for bugs and issues
4. IDENTIFY potential problems in:
   - Packet crafting logic
   - Sequence number calculations
   - Threading and synchronization
   - Error handling
   - Network interface handling
5. FIX any issues found through code analysis
6. Execute the attack command in WSL with debugging
7. If attack fails, DIAGNOSE the problems and iterate fixes
8. Verify successful YouTube disruption only after code is working
9. Be prepared to rewrite components if necessary

DEBUGGING APPROACH:
==================
- Start with component testing (ARP poisoning alone, packet monitoring alone)
- Add verbose logging and debug output
- Test with simple targets before complex YouTube traffic
- Validate packet contents and network behavior
- Monitor system logs for error messages
- Use network analysis tools (Wireshark equivalent) if needed

SAFETY NOTES:
============
- Attack targets private network (192.168.137.x) ✅
- Educational purpose (BUET CSE406) ✅
- User owns both devices (laptop + phone) ✅
- RFC 1918 validation enabled ✅
- Time-limited execution (3 minutes) ✅

FINAL NOTE:
==========
This TCP Reset Attack project has a solid foundation but should be considered
a WORK IN PROGRESS that needs thorough testing and likely debugging in a 
Linux environment. The AI agent should be prepared to:

- Analyze and fix bugs in the existing code
- Add missing functionality if discovered
- Rewrite components that don't work as expected
- Iterate through multiple testing and debugging cycles
- Help achieve a working attack through systematic problem-solving

The user has done significant development work, but the project needs an
experienced AI agent to carry it across the finish line with proper
debugging, testing, and execution in WSL.
